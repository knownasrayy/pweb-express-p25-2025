// src/modules/transaction/transaction.routes.ts
import { Router } from 'express';

const router = Router();

// Endpoint akan ditambahkan di sini nanti
// router.post('/', ...);
// router.get('/', ...);

export default router;